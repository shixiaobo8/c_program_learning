#include <stdio.h>
// 求解方程  ax*x+b*x+c=0

int getX(int a,b,c)
{
	if(a==0){
		printf("该方程式不是一个一元二次方程,不符合题干要求\n");
	}else{
		
	}
}

int main()
{
	int a,b,c;
	printf("请输入方程ax*x+b*x+c=0的系数a,b,c的值:");
	scanf("%d,%d,%d",a,b,c);
	getX();
}
